FROM oven/bun:1.4.2

WORKDIR /app

# Install dependencies first for layer caching
COPY package.json bun.lock ./
RUN bun install --frozen-lockfile

# Copy application source
COPY . .

EXPOSE 3000 5173

# Default dev command runs both API and web with hot reload
CMD ["bun", "run", "dev:api"]
